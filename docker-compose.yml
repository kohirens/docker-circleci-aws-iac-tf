version: "3.8"

services:
    container:
        image: "kohirens/circleci-aws-iac-tf:dev"
        build:
            dockerfile: "Dockerfile"
            context: "."
            target: "dev"
        tty: true
